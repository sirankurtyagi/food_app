import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const Filters = props => {
    
    return (
           <View><Text>Filters Screen</Text></View>
            );
};

Filters.navigationOptions = {
    headerTitle : 'Filters'
};

const styles = StyleSheet.create({
    
});

export default Filters;